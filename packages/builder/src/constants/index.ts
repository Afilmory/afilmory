// 支持的图片格式
export const SUPPORTED_FORMATS = new Set([
  '.jpg',
  '.jpeg',
  '.png',
  '.webp',
  '.bmp',
  '.tiff',
  '.heic',
  '.heif',
  '.hif',
  '.tif',
])

// HEIC/HEIF 格式
export const HEIC_FORMATS = new Set(['.heic', '.heif', '.hif'])

// 支持的 3D 场景文件（Gaussian Splat）
export const THREE_D_SCENE_EXTENSIONS = new Set(['.sog'])
